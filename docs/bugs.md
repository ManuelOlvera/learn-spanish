SHIPPED
- ~~Ensure the balloon game is available for all kids, my boy likes it too~~ ✅ shipped 2026-08-13: el globo 🎈 follows la sopa — the kid gate is gone, the only gate left is whether the deck's words fill a length band. Both kids see it on every deck that offers it (see shipped.md)
- ~~The accessories disappear when you move to another mascota~~ ✓ decided 2026-08-04: NOT a bug — dressing is per-pet on purpose. Owning a crown does not put it on every mascota; each one is dressed deliberately, so a switched-to or newly adopted pet starts bare and the kid picks its outfit from el armario. Nothing is lost: ownership is kid-level and every owned accessory is always there to tap on. Briefly "fixed" this by making an undressed pet wear the whole wardrobe (the `PetState.worn` doc comment claimed that was the intent) and reverted the same day — **do not re-apply**. The stale comment that caused it is corrected; the real bug in the same area was `placements` (below). *Refined 2026-08-13:* outfits are now per **shape** as well as per pet (`PetState.outfits`, keyed by form) — a hat on the grown hen no longer floats over the egg. Same rule as before, one level finer: a shape the kid has not dressed is bare, including a form the pet has just grown into.
- ~~The dragged accessory spots reset~~ ✅ fixed 2026-08-04: `mergePet` never returned `placements`, so every sync dropped where the kid had dragged each accessory and they snapped back to their default spots. Now merged per accessory (both devices keep their spots, receiving device wins a conflict), like worn/form (`domain/transfer.ts`).
- ~~When you feed the animals quickly the sad face comes back sometimes~~ ✅ fixed 2026-08-04: not the feeding — the merge. `mergeProgress` resolved a pet collection's `active` incoming-wins (`incomingCol.active || existing.active`), while its two siblings in the same function (`worn`, `form`) are deliberately receiving-device-wins because they are per-device display choices. `active` is the same kind of thing, so every pull adopted the OTHER device's pet: feed the pollito on the phone, then any pull (app open, and every `visibilitychange`) swapped the screen to the tablet's conejo, unfed for weeks — hungry face back. Rapid feeding made it frequent because each meal fires a `syncPush`, so a stale union was almost always in flight. Now the receiving device keeps its own pet and the incoming value only fills a gap (a freshly paired device still opens on a real pet); meals/owned still converge both ways (`domain/transfer.ts`).
- ~~The kids sometimes forget to open the chest and don't get the stars — especially in el reto~~ ✅ fixed 2026-08-01: stars were only credited on the chest tap, so leaving the done screen with it shut dropped the whole round. Reto was the loud case (🔁 sits right under the chest, the récord chase invites replay spamming, and a 60s run is the biggest haul). The chest now banks on unmount too, and the exits dim while it's closed (see shipped.md).
- ~~Accessories don't stay on the mascot after you move to another page~~ ✅ fixed 2026-07-22: `mergePet` was pruning `worn` against the legacy per-pet `accessories` list, but ownership is kid-level now so that list is empty — every sync merge stripped the outfit. It only prunes when there's a legacy list to prune by (`domain/transfer.ts`).
- ~~When you finish El repaso the review badge doesn't go away~~ ✅ fixed 2026-07-22: repaso re-asks each weak word once and a wrong weighs double, so one correct answer could never offset the miss that flagged it. Review mode now heals a prior miss on a correct answer (`recordReviewAnswer`), so a good pass can actually clear the word.
- ~~Duolingo-like trail (easy → hard path through the content)~~ ✅ shipped 2026-08-22 as **El camino 🧭**: both levels the fork asked about (the shelves are the spine, each shelf's decks its own path), drawn on home and the shelf screen with no new page, and it never locks — the free-play tension resolved by making the route a suggestion, not a gate (ADR 016; see shipped.md)
- ~~For the alphabet, don't say la a, la b, la c... just say the letter otherwise is confusing~~ ✅ fixed 2026-07-15: letter cards now speak the bare name ("be"); the article moved to a new `article` field so scene still asks "¿Dónde está la be?" (see shipped.md)
- ~~Make sopa de letra available for both kids. Both kids like it~~ ✅ shipped 2026-07-15: the kid gate is gone; the only gate left is whether the deck's words fit a grid (see shipped.md)
- ~~add ABC category for kids to learn letters~~ ✅ shipped 2026-07-14: Las letras shelf (vocales + full alphabet); letter games on the roadmap
- Profile should have age bracket which you use to display relevant games → parked as roadmap #18 (listener→reader upgrade path) per 2026-07-14 discussion
- ~~Spacing numbers in iPad is not working properly. numbers are bigger than the square. works fine on my phone~~ ✅ fixed 2026-07-14 (wide keycap emoji now size down; see shipped.md)
- ~~Add hundreds, thousands,millions to numbers~~ ✅ shipped 2026-07-14: Las centenas (100–1000); miles/millones deferred to roadmap #17
- ~~Syncing doesn't seem to sync properly. On the phone my daughter has all accesorios bought but not all show as available on the tablet~~ ✅ fixed 2026-07-14 (purchases now push immediately; see shipped.md) — after updating, buy anything or finish a game on the phone once, then open the tablet
- ~~The chest is like a workaround to get cheap accessories. Maybe we need to rethink it~~ ✓ decided 2026-07-14: keep as-is (the box is joy, prices are theater) — roadmap #19 records the reasoning
- ~~a new feature, add the game Squaredle with hidden words within a matrix. matrix cells depend on difficulty (easy, medium, hard)~~ ✅ shipped 2026-07-14 as La sopa de letras 🥣 (reader-level, 6×6/7×7/8×8 by difficulty); bent-path "true Squaredle" selection parked as roadmap #20